/**
 * Konfigurierbare Plattform-Liste für scripts/sync-social-stats.js.
 * Neue Plattform aufnehmen = hier ergänzen; das Widget zeigt automatisch
 * jede Zeile aus der Tabelle "social_stats" an.
 */
module.exports = {
  platforms: ["x", "youtube", "instagram"],
};
